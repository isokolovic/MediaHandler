# MediaHandler — C++ CLI Media Compressor (v2)

**Modern rewrite: Multithreaded, resumable compression.**

## Quick Start
```bash
git clone https://github.com/isokolovic/MediaHandler.git
cd MediaHandler
./build.sh  # or build.bat (auto-deps)
./bin/mediahandler ~/Photos ~/Backup -j auto -r -o --profile default
